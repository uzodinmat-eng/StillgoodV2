# Leave empty to use local PGlite at `.data/stillgood` (Postgres-compatible).
# After you run supabase/migrations + supabase/seed.sql in the Supabase SQL editor,
# paste the Database URI here (Settings → Database). Direct or session pooler both work.
DATABASE_URL=
